#!/bin/sh

  s=$(curl $1 | grep /update/live | sed -r 's/.* href="([^"]+)" .*/\1/' | sed -r 's/.* src="([^"]+)" .*/\1/'| head -n 1 | tail -1 )
  url=http://cricfree.tv$s
  echo $s
  
  firefox $url &

 #~/scripts/rtmp.sh 